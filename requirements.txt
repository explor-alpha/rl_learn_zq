# requirements.txt


# --- 基础工具 (使用 >= 保证跨平台兼容性) ---
numpy>=2.0.0
scipy>=1.15.3
matplotlib>=3.10.8
pillow>=11.3.0
tqdm>=4.67.3
psutil>=7.2.2
pyyaml>=6.0.3
setuptools>=69.5.1

# --- 开发与记录工具 ---
jupyter>=1.1.1
ipykernel>=7.2.0
tensorboard>=2.20.0

# --- 物理引擎与环境 (严格对齐版本，保证仿真一致性) ---
# 包含 mujoco 3.6.0 接口
gymnasium[mujoco,classic-control,other]==1.2.3
panda-gym==3.0.7

# --- 算法标准化实现 (核心逻辑，必须严格对齐) ---
# 该版本支持最新的 gymnasium 和 torch
stable-baselines3[extra]==2.8.0